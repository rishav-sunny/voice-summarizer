[project]
name = "voice-summarizer-backend"
version = "0.1.0"
description = "FastAPI backend for real-time transcription and summarization using AssemblyAI"
requires-python = ">=3.10"

[tool.pdm]

[tool.poetry]

[tool]

